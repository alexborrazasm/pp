funcion sys.time, pregunta cuantos segundo lleva consumidos el so de cpu
la llamas ejecutas lo que quieras y luego la vuelves a llamar y restas.

let crono f x = (* Función automatizada para medir el tiempo de una función *)
    let t = Sys.time () in
    let _ = f x in
    Sys.time () -. t;;

val crono : ('a -> 'b) -> 'a -> float = <fun>

let crono f x y = (* Función automatizada para medir el tiempo de una función que tome 2 argumentos *)
    let t = Sys.time () in
    let _ = f x y in
    Sys.time () -. t;;
